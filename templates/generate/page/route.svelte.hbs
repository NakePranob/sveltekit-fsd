<!-- Routing only. The page's own <svelte:head> carries its title, so this file
     has nothing to forward but the component itself. -->
<script lang="ts">
	import { {{pascal}}Page } from "{{pageAlias}}/pages/{{directory}}";
</script>

<{{pascal}}Page />
